# Work Order: Sanitization Batch 3 - Project Chapters

## Objective
Clean tokenization artifacts and ghost characters in the project implementation chapters.

## Target Files
All `.tex` files in `capitulos/proyecto/`.

## Protocol
1. `read_file` $ightarrow$ 2. Clean in memory $ightarrow$ 3. `write_file`.