\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{config}[2026/03/10 Gustavo Ferreyra]

\RequirePackage[spanish]{babel}
\RequirePackage[american]{circuitikz}
\RequirePackage{../common/sty/iramA4,amsmath,pgfplots,newtxtext,newtxmath,steinmetz}
\RequirePackage{sectsty,longtable,enumitem,tikz,rotating}
\RequirePackage[toc]{glossaries}
\RequirePackage{../common/sty/toolsgf,cancel,titletoc,subfiles}
\RequirePackage{fancyhdr,geometry,pdfpages,titlesec}

\makeglossaries
\usetikzlibrary{shapes,arrows,decorations.text}
\tikzstyle{line}=[draw] % here
\setlist[itemize]{leftmargin=*}

\makeatletter
\let\ps@plain\ps@empty
\makeatother
\pagestyle{empty}

\newglossaryentry{formula}
{
  name=formula,
  description={A mathematical expression}
}
\sectionfont{\sffamily\Large}
\subsectionfont{\sffamily}

%\allsectionsfont{\sffamily}
\pgfplotsset{compat=1.7}

\newcommand{\materia}{ELECTROTECNIA II}
\newcommand{\TP}{}
\newcommand{\logo}{images/logos/4124}
\newcommand{\email}{gustavoferreyra@outlook.com}
\newcommand{\classroomcode}{.......}
\newcommand{\urlqr}{https://es.scribd.com/document/528527875/}
\title{ \logoo \\ \Huge Guía de Trabajos Prácticos\\  \textbf{\materia}}
\author{Ferreyra G.}
\date{}
\sectionfont{\sffamily}
\subsectionfont{\sffamily}
\titleformat{\chapter}{\sffamily\huge}{\textbf{Trabajo Práctico
N$^\circ$:\thechapter}}{20pt}{\centering}

\newcommand{\logoo}{
  \begin{tikzpicture}[scale=1]
    \fill[color=white] (0,0) circle (6);
    \foreach \x in {1,...,36}
    \draw[line width=2mm] (\x*10:3.5cm)--(\x*10:4cm);
    \foreach \x in {1,...,36}
    \draw[line width=4mm] (\x*10:3.5cm)--(\x*10:3.3cm);

    \foreach \x in {1,...,36}
    \draw[gray,thin] (0,0)--(\x*10+2:3.3cm);

    \foreach \x in {1,...,36}
    \draw[gray,thin] (0,0)--(\x*10-2:3.3cm);

    \foreach \x in {1,...,36}
    \draw[gray,line width=5mm] (\x*10:5.9cm)--(\x*10:5.7cm);

    \foreach \x in {1,...,144}
    \draw[line width=1mm,gray] (\x*2.5:6cm)--(\x*2.5:6.3cm);

    \draw [line width=0.1mm] (0,0) circle (6.3cm);
    \draw [line width=3mm](0,0) circle (6cm);
    \draw [line width=3mm](0,0) circle (4cm);

    \path [postaction={decoration={
          text along path,
          text align=fit to path,
        text={|\fontsize{35}{35}\selectfont\sffamily| ESCUELA TECNICA 4-124} },
    decorate}]    (-4.5cm,0) arc (180:0:4.5cm);
    %(-5,0) arc [start angle=180,end angle=0,radius=5];
    \path [postaction={decoration={
          text along path,
          text align= center,
          text={ |\fontsize{40}{40}\selectfont| REYNALDO MERIN}
    }, decorate}](-5.5cm,0) arc (180:360:5.5cm);
    %(-5,0) arc [start angle=180,end angle=0,radius=5];
    \fill [line width=3mm](0,1) circle (1.8cm);
    \draw [line width=4mm](-1,-1.3)--(1,-1.3);
    \draw [line width=4mm](-0.8,-1.5)--(0.8,-1.5);
    \draw [line width=3mm](-.7,-1.9)--(.7,-1.6);
    \draw [line width=3mm](-.7,-1.9-.4)--(.7,-1.6-.4);
    \draw [line width=1mm](-.7,-1.9-.4)--(.7,-1.9-.4);
    \draw [line width=3mm](-.8,-1.5) arc (0:30:3cm);
    \draw [line width=3mm](.8,-1.5) arc (180:150:3cm);
    \draw [line width=3mm](-.3,-2.5)--(.3,-2.5);
    \draw [line width=7mm](-1,-1)--(1,-1);
    \draw [white] (0,0) node {\fontsize{80}{80}\faFlash};
  \end{tikzpicture}
}
\endinput
